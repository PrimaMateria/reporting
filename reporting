WATSON="$HOME/apps-terminal/homebrew/bin/watson"

PROJECT=$1
REPORTING_BASE_DIR="$HOME/Documents/Reporting"
PROJECT_DIR="${REPORTING_BASE_DIR}/${PROJECT}"

WATCH_LOG="watchsh \"$WATSON log -a -p $PROJECT\" 5"
WATCH_REPORT="watchsh \"$WATSON report -a -p $PROJECT\" 5"

if [! -d $PROJECT_DIR ] 
then
  mkdir $PROJEECT_DIR
fi

echo $WATCH_LOG
$("watchsh \"echo test\"")


#tmux new-window -c $PROJECT_DIR -n $PROJECT $WATCH_LOG 
#tmux set-option -w remain-on-exit on
